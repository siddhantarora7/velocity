# FastAPI ML backend. Heavy deps (torch, opencv) come in transitively via
# ultralytics — this service is meant for a container host, NOT a serverless
# function. opencv-python-headless avoids GUI/libGL deps on servers.
fastapi
uvicorn[standard]
python-multipart
sqlalchemy
# bcrypt pinned to the last version passlib 1.7.4 supports; 4.1+/5.x break it
# (removed `__about__`, and now raise on the 72-byte probe). Do not unpin.
passlib==1.7.4
bcrypt==4.0.1
python-jose[cryptography]
python-dotenv
psycopg2-binary
ultralytics
opencv-python-headless
numpy<2
filterpy
matplotlib
