# Copy to .env.local for dev, or set in Vercel project settings for prod.
# Points the frontend at the deployed backend (Render). Omit it to use localhost.
VITE_API_BASE=https://velocity-api.onrender.com
